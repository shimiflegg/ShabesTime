<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shimi.shabestime">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="זמני שבת"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main2Activity" />
        <activity android:name=".ViewDatabase" />
        <activity android:name=".datetimefromack2" />
        <activity android:name=".dataShabbathForMack1" />
        <activity android:name=".NewShabbathActivity" />
        <activity android:name=".ShabbathDetailsActivity"></activity>
    </application>

</manifest>